interface HomeProps {
  params: {
    locale: string;
  };
}

interface HeaderProps {
    t: {
      about: string;
      projects: string;
      contact: string;
      language: string;
      english: string;
      spanish: string;
      italian: string;
  
    };
  }